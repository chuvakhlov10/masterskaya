{
  "name": "masterskaya",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:functions": "node scripts/build-yandex-functions.mjs",
    "verify:functions": "node scripts/verify-yandex-bundles.mjs",
    "preview": "vite preview",
    "test": "node --experimental-default-type=module --test tests/*.test.mjs",
    "check": "npm run test && npm run build && npm run build:functions && npm run verify:functions"
  },
  "dependencies": {
    "ably": "^2.23.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
